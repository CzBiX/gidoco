services:
  gidoco:
    build: .
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      # must be the same absolute path on both host and container
      - /root/repo:/root/repo
      - /var/run/docker.sock:/var/run/docker.sock
